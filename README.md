# PaymentService

A production-grade payment service built in Go.

Supports pluggable payment providers (Stripe, PayPal, Razorpay) without changing core logic. Handles failures gracefully, prevents double charges, and is designed to scale to millions of transactions.

## Structure

```
Learning/        — Go concepts learned while building this project
PaymentService/  — Go source code
```
